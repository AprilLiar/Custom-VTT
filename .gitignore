node_modules/
client/dist/
local.db
# Left over from the embedded replica, which is gone (see server/db.js). Kept
# ignored so a checkout that still has one lying around never commits it.
replica.db
replica.db-*
.env
test-results/
playwright-report/
